import { buildSeries } from './helpers';
export const prebuiltProducts = buildSeries(1000, 10, 'prebuilt', 'Computer Buddy', 'Budget Battlestation', 3999, { Socket: 'AM5', TDP: 105, GPU: 'RTX 4070', RAM: '32GB DDR5' });
