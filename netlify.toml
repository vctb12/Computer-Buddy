[build]
  base = "apps/web"
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

# Static export already outputs concrete HTML routes.
# Keep redirects minimal and valid to avoid conflicting with Next routing.
[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200
