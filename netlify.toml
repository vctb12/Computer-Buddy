[build]
  base = "/"
  command = "corepack enable && pnpm -w install --frozen-lockfile && pnpm --filter @computer-buddy/web run build"
  publish = "apps/web/out"

[build.environment]
  NODE_VERSION = "20"